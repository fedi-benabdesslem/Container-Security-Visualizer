# Container Security Visualizer - Environment Configuration

# --- Backend Settings ---
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8002
DEBUG=false
RELOAD=false

# --- Database Settings ---
DB_TYPE=postgresql
DB_HOST=db
DB_PORT=5432
DB_NAME=container_security
DB_USER=postgres
DB_PASSWORD=datasec1

# --- CORS Settings ---
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# --- Collector Settings ---
COLLECTOR_OUTPUT_MODE=http
COLLECTOR_API_ENDPOINT=http://backend:8002/api/events
CACHE_TTL_SECONDS=30
